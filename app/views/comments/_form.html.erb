<%= form_with model: [ post, post.comments.build ], local: false do |form| %>
  <div>
    <%= form.text_field :content, placeholder: "Add comment here..." %>
    <%= form.hidden_field :user_id, value: current_user.id %>
  </div>
  
  <div>
    <%= form.submit "Add comment" %>
  </div>
<% end %>